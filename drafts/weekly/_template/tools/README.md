# Tool Drafts

Use this folder for durable interactive tools or tool pages.

Do not create a public `tools/<slug>.html` page for ordinary news. A tool page should be reusable after the weekly issue is over.

Suggested tool brief:

```markdown
# Tool name

Purpose:
Audience:
Inputs:
Outputs:
Core interactions:
Links from issue:
```
